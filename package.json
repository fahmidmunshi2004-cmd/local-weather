{
  "private": true,
  "scripts": {
    "dev": "npm --prefix new-project run dev",
    "build": "npm --prefix new-project run build",
    "preview": "npm --prefix new-project run preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "axios": "^1.17.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^4.3.0"
  }
}
